Improperly newline-separated ``keywords`` and ``platforms`` are now handled by treating each line as a separate item, and that usage is now deprecated. Newlines were never a valid separator for these fields -- the `old specification <https://peps.python.org/pep-0345/>`_ separated items with spaces and the current one uses commas -- and they corrupted the generated metadata (pypa/setuptools#4887).
