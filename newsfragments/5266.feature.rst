The C compiler modules now emit log messages through their own module-level ``logging.getLogger(__name__)`` loggers instead of the distutils root logger, part of decoupling the compilers package from distutils.
